<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.READ_PHONE_STATE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
                   android:usesPermissionFlags="neverForLocation" />
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
                   android:usesPermissionFlags="neverForLocation" />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

  <!--
    CompanionDeviceManager.associate throws IllegalStateException without this declaration.
    Optional so the app still installs on devices without companion device setup; the
    Bluetooth route simply stays unavailable there.
  -->
  <uses-feature
      android:name="android.software.companion_device_setup"
      android:required="false" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

  <queries>
    <intent>
      <action android:name="android.intent.action.MAIN" />
      <category android:name="android.intent.category.LAUNCHER" />
    </intent>
  </queries>

  <application>
    <service
      android:name="co.twinotify.core.listener.TwinotifyNotificationListener"
      android:label="@string/app_name"
      android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
      android:exported="true">
      <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
      </intent-filter>
    </service>

    <service
      android:name="co.twinotify.core.service.SyncService"
      android:foregroundServiceType="remoteMessaging|connectedDevice"
      android:exported="false" />

    <receiver
      android:name="co.twinotify.core.service.BootReceiver"
      android:enabled="true"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>

    <activity
      android:name="co.twinotify.core.detail.NotificationRouterActivity"
      android:excludeFromRecents="true"
      android:exported="false"
      android:noHistory="true"
      android:theme="@android:style/Theme.Translucent.NoTitleBar" />

    <receiver
      android:name="co.twinotify.core.service.MaterializationRetryReceiver"
      android:exported="false" />

    <receiver
      android:name="co.twinotify.core.actions.ActionClaimRecoveryReceiver"
      android:exported="false" />

    <receiver
      android:name="co.twinotify.core.actions.ActionInvokeReceiver"
      android:exported="false" />

    <receiver
      android:name="co.twinotify.core.call.CallControlInvokeReceiver"
      android:exported="false" />

    <receiver
      android:name="co.twinotify.core.actions.ActionInvocationExpiryReceiver"
      android:exported="false" />
  </application>
</manifest>
