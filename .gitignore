# Synced from /proto via `make sync-proto` — never committed.
relay/internal/server/schemas/
relay/internal/server/fixtures/

mobile/android/
mobile/ios/
mobile/modules/twinotify-core/android/build/

# Local execution state for the subagent-driven implementation workflow.
.superpowers/
.omo/evidence/
.worktrees/

# Local production deploy audit record (image digests only, generated by deploy-relay.sh).
deploy/.relay-deploy-state
bin/relay
